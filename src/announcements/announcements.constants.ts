// Mutual-aid death assessment: every active member (except the deceased)
// contributes a base amount, plus the same amount again for each of their
// own registered beneficiaries.
export const DEATH_ASSESSMENT_BASE_AMOUNT = 10;
export const DEATH_ASSESSMENT_PER_BENEFICIARY_AMOUNT = 10;

export const MAX_ANNOUNCEMENTS_PER_PAGE = 50;
export const MAX_ADMIN_ANNOUNCEMENTS_PER_PAGE = 100;
